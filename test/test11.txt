fun main(): Unit {
    var outer: Int = 0
    var total: Int = 0

    while (outer < 3) {
        var inner: Int = 0

        while (inner < 2) {
            total += outer + inner
            inner++
        }

        outer++
    }
    println(total)

    println("Test control while-anidados completado")
}